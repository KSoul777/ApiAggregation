namespace Aggregator.Aggregation.Application.Feed;

public enum FeedSortField
{
    Timestamp = 0,
    Relevance = 1,
    Source = 2,
    Category = 3,
    Title = 4
}

public enum SortDirection
{
    Descending = 0,
    Ascending = 1
}
